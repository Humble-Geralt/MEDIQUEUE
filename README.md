# MediQueue

## 1. 环境描述

### 1.1 开发环境描述
- Python 版本：`3.12.0`
- Node.js 版本：`v24.15.0`
- 当前环境额外配置的 Codex Skills：
  - `canvas-design`
  - `frontend-design`
  - `harness engineering`
- 当前环境可用的 MCP 服务：
  - `mcp__codex_apps__github`
  - `mcp__node_repl__`
- AI工具：
  - `Codex`: 主力开发
  - `Gemini CLI`: 杂活
  - `Google AI studio`：主要生成初版前端
  - `ChatGPT网页版`: 开发过程中的一些碎屑问题

## 2. 业务分析

本节对应题目中的 `交付物 A：业务分析文档`，内容完全基于 [native_description.md](./docs/native_description.md) 的 5 条原始反馈提炼，不额外假设医院已有复杂后台系统或多角色管理端。

完整的prd分析、设计风格和架构设计可以参考 
- [prd_v1.md](./docs/prd_v1.md)
- [prd_v2.md](./docs/prd_v2.md)
- [design.md](./docs/design.md)
- [backend_api_architecture.md](./docs/backend_api_architecture.md)
- [frontend_architecture.md](./docs/frontend_architecture.md)

### 2.1 A1. 痛点提炼与用户画像

#### 2.1.1 用户画像与核心诉求

| 角色 | 代表反馈 | 主要特征 | 核心诉求 |
| --- | --- | --- | --- |
| 老年患者 | 王大爷 | 视力一般、对高密度信息不友好、对过号成本敏感 | 看得清当前号码、听得见提醒、过号后不至于完全失控 |
| 外籍患者 | David Smith | 不懂中文、需要独立完成候诊 | 看得懂英文界面、听得懂英文播报、少依赖他人帮助 |
| 特殊紧急患者 | 李女士 | 症状明显、当前系统无法动态调序 | 可以发起紧急优先申请，并尽快被医生注意到 |
| 医生 | 赵主任 | 需要稳定问诊节奏、厌恶被频繁打断 | 队列清晰、叫号可控、患者不要反复敲门追问 |
| 现场工作人员/护士 | 张姐 | 真实承担秩序维护，但本轮不建设独立系统端 | 在断网和争议场景下有清晰的人工兜底方式 |

#### 2.1.2 从反馈到问题归类

| 反馈来源 | 原始问题 | 归类后的产品问题 |
| --- | --- | --- |
| 王大爷 | 大屏太密、广播听不清、过号后等待过长 | 公共信息可读性差、提醒通道单一、过号体验差 |
| David Smith | 屏幕和播报都无法理解 | 系统缺少英语支持 |
| 李女士 | 紧急患者无法通过系统被优先处理 | 队列缺少动态优先机制 |
| 赵主任 | 患者不断打断问诊节奏 | 队列透明度低、医生端缺少有效控制面板 |
| 张姐 | 插队难解释、真假紧急难判断、断网后系统瘫痪 | 缺少优先申请判定机制、缺少断网降级方案 |

#### 2.1.3 归纳出的核心功能需求

1. 必须有一个实时同步的统一队列，让三端看到的是同一状态。
2. 必须把“当前叫号”做成最强视觉信息，大屏端能被远距离识别。
3. 必须支持中英文界面，至少让外籍患者能理解关键状态。
4. 患者端必须能看到“我的号码、前方人数、预计等待时间、诊室号”。
5. 医生端必须支持“呼叫下一位、跳过当前、暂停/恢复叫号”。
6. 患者端必须提供“紧急优先申请”入口。
7. 紧急优先申请不能自动插队，而是先经 AI 分析，再由医生决定批准或驳回。
8. 断网时不能假装正常运行，必须显式展示“当前断网”和“断网前最新状态”。

#### 2.1.4 本轮 MVP 的建模结论

基于题目限时交付约束，本轮系统只建设：

- 医生端 Web
- 候诊大屏端
- 患者端 H5
- 后端 API + WebSocket 服务

虽然护士长的反馈暴露了真实业务痛点，但本轮不单独建设护士端；相关问题在 MVP 中被折叠为：

- 医生端审核紧急优先申请
- 大屏/患者端在断网时提示“听从现场工作人员或护士指引”

### 2.2 A2. 功能优先级矩阵

#### 2.2.1 P0：MVP 必须完成

| 功能 | 说明 | 分级理由 |
| --- | --- | --- |
| 队列实时同步 | 医生端、大屏端、患者端状态一致 | 这是叫号系统成立的前提，没有同步就没有多端系统 |
| 医生端叫号/跳过/暂停/恢复 | 医生推进当前诊室的叫号流程 | 直接决定主流程能不能跑通 |
| 患者端个人排队信息 | 展示号码、前方人数、预计等待时间、诊室号 | 直接缓解焦虑与重复询问，是患者端存在的核心价值 |
| 候诊大屏高可读展示 | 大字号、高对比、当前叫号高亮 | 设计问题，直接针对“王大爷”看不清的问题 |
| 中英文界面 | 关键页面与状态支持英文理解 | 直接回应 David 的需求，且改造成本相对可控 |
| 页面刷新后状态恢复 | 任意端刷新后恢复当前正确状态 | 保证状态一致性，符合题目技术要求 |
| 紧急优先申请入口 | 患者可提交一句话描述 | 紧急情况的患者，可以直接用这个，否则还是要找护士长，从功能来看似乎可以排P1，但医疗场景下人命关天，所以提到P0|

#### 2.2.2 P1：重要但可延后

| 功能 | 说明 | 分级理由 |
| --- | --- | --- |
| 双语语音播报 | 中文后英文播报当前叫号 | 语音播报比较重要，能够在现场直接告知患者情况 |
| AI 结构化评估能力 | 为优先申请提供辅助判断 | 很重要，但实际是一种预审，如果不可用或者没有，也可以直接把患者输入给医生，只是会增加时间|
| 断网检测与离线降级展示 | 断网时显式提示并保留断网前最新状态 | 对安全很重要，但在最小演示闭环中可晚于主叫号流程落地 |
| 过号重返队列策略 | 患者过号后如何重新回到队列 | 真实高频，但规则复杂，可先人工处理再产品化 |

#### 2.2.3 P2：锦上添花

| 功能 | 说明 | 分级理由 |
| --- | --- | --- |
| 更细粒度隐私模式 | 更多姓名脱敏与展示策略 | 当前固定脱敏规则已能满足 MVP |
| 预计等待时间智能预测 | 基于更复杂模型动态估算 | MVP 先用简单规则估算即可 |
| 患者满意度收集 | 候诊结束后反馈 | 不影响核心业务闭环 |

### 2.3 A3. 离线降级方案

本方案专门针对反馈 #5 中“内网交换机故障 40 分钟导致系统瘫痪”的问题设计。本轮不追求断网时继续复杂自动写操作，而是优先保证：

- 各端第一时间知道系统已断网
- 各端继续展示断网前最后一版有效状态
- 患者能获得统一、稳定、双语的安抚与引导信息

#### 2.3.1 断网检测机制

系统通过以下三类信号判断是否进入断网状态：

1. WebSocket 心跳超时
2. WebSocket 连接断开且多次自动重连失败
3. 获取最新队列快照的接口连续失败

判定原则：

- 满足任一关键条件即可切换到断网展示模式
- 前端不继续假设“只是短暂卡顿”，而是显式进入异常态

#### 2.3.2 降级运行策略

##### 医生端

- 页面顶部以高对比 Banner 显示“当前网络异常”
- 继续保留断网前最后一次成功同步的队列快照
- 明确提示“当前数据可能不是最新，请结合现场情况判断”

##### 候诊大屏端

- 保留断网前最后一版有效队列状态
- 显示显眼的断网标识
- 循环显示双语公告：
  - 中文：`当前系统网络故障，请不要紧张，听从现场工作人员或护士指引`
  - 英文：`The queue system is temporarily offline. Please stay calm and follow the instructions of on-site staff or nurses.`

##### 患者端

- 显示“网络连接异常”提示
- 保留断网前最后一次个人候诊状态
- 提示患者不要仅依赖手机数据，需留意现场工作人员指引

#### 2.3.3 恢复后数据同步策略

网络恢复后采用“重新对齐快照”的策略，而不是复杂的离线补写：

1. 客户端自动重连 WebSocket
2. 重连成功后立即重新拉取完整队列快照
3. 用服务端最新快照覆盖断网期间保留的旧状态
4. 各端重新进入正常同步模式

这样做的原因是：

- 当前 MVP 不建设复杂离线写入和冲突合并机制
- 在医疗场景里，错误同步比延迟恢复更危险
- 对于限时题目，优先保证“状态可见、恢复清楚”比“离线强行可写”更稳妥

#### 2.3.4 后续一些优化可能

接入redis存储基于年龄区间、性别、国籍相关的key，value为平均诊断耗时。

ws定时推送这些kv对，当出现中断的时候，各个终端实际上还是可以通过这些历史数据进行预估，判断队列流转状态。

例如断网后，用户端可以根据前序的患者数量 * 对应的平均耗时，拿到一个期望值，在终端自行进行排队状态的预测，显示预估的还需排队多长时间

### 2.4 A4. AI 使用说明

#### 2.4.1 本项目中使用 AI 的环节

本项目在以下环节使用了 AI 工具辅助：

| 阶段 | AI参与占比 | 预估耗时 | 是否并行 | 主要工作 |
| --- | --- | --- | --- | --- |
| 需求理解 | 0% | 10min | 否 | 主要是阅读原始题目，自行理解和梳理整体需求，以及一些重要注意点 |
| 需求梳理、PRD生成与讨论 | 75% | 45min | 否 | 详细拆解原始需求，通过AI整理初版PRD，人工审核调整后再生成下一版本PRD，直到需求边界和主要功能梳理清晰 |
| 开发上下文生成 | 90% | 15min | 否 | 通过终版PRD，生成开发相关的必要上下文，例如UI/UX设计要求、前后端架构设计、复杂项目的多服务架构图（本考题不涉及）等 |
| 前端代码生成、自测调试与审核 | 80% | 1hr | 是 | AI studio生成初版，后续连续调整 |
| 后端代码生成、自测调试与审核 | 80% | 20min | 是 | Codex生成初版，后续连续调整 |
| 前后端联调%调整 | 90% | 2hr | 否 | 移除mock部分，打通前后端，中间的多次代码调整，接chrome dev tool mcp调试反馈 |
| 人类验收与细节微调 | 20% | 20min | 否 | 主要测试主线流程通不通以及bug点处理 |
| 文档整理 | 50% | 10min | 否 | 整理各类文档，harness上下文，后续代码落地中间产生的变更部分 |
| 部署测试 | 0% | 20min | 否 | 在另一台电脑上完整部署和简单冒烟测试 |

#### 2.4.2 如何使用

AI 负责：
- prd的解析、整理以及必要的开发上下文的初步提取
- 具体的代码落成，实践
- 一些可以在编码中察觉的bug、问题代码清理
- 简单项目的架构设计

人工负责：
- 配置agent需要的开发环境、skill、mcp等等
- 校验必要的开发上下文的正确性，例如`.\docs`文件夹内的各类文件
- 中断一些错误的AI操作，并引导
- 后期的整体流程测试，冒烟
- 部署测试

#### 2.4.3 如何判断分工

- 明确的、简单但大量的、无二义性的、规模小的、改动偏上层的、有现成样例的 -> AI
- 复杂的、创新型的、业务依赖性强的、与人打交道的、安全性高的、有较高代码质量要求的 -> 人类

其实最核心的就是AI来开头和实施，人类在整个实施流程的过程中有效的监控和中断。在涉及当下agent能力边界的问题时，给予足够的帮助和上下文。识别agent工作的正确性。

## 3. Quick Start

本项目提供了**一键启动脚本**，可以自动引导配置 API Key 并拉起全栈服务。

### 3.1 一键启动 (推荐)

- **Windows (PowerShell)**:
  ```powershell
  ./run_mediqueue.ps1
  ```
- **Linux / macOS (Bash)**:
  ```bash
  chmod +x run_mediqueue.sh
  ./run_mediqueue.sh
  ```

**脚本功能：**
1. **自动配置**：首次运行会提示输入 `DEEPSEEK_API_KEY` 并自动保存到 `api/.env`。
2. **全栈拉起**：自动启动 FastAPI 后端（端口 8000）及四个前端视图入口。
3. **一键清理**：按下 `Ctrl+C` 即可同时停止后端和所有前端进程。

### 3.2 访问地址预览

启动后，您可以访问以下地址：

| 终端 | 访问地址 | 说明 |
| --- | --- | --- |
| **演示中心** | [http://localhost:5176](http://localhost:5176) | **主力演示页**：一屏观察三端实时联动 |
| **医生端** | [http://localhost:5173](http://localhost:5173) | 独立医生工作站：叫号、跳过、审核 |
| **候诊大屏** | [http://localhost:5174](http://localhost:5174) | 独立大屏：双语播报、队列高亮 |
| **患者端** | [http://localhost:5175](http://localhost:5175) | 独立手机端：查看进度、申请加急 |

---

### 3.3 手动分步启动 (进阶)

如果您需要调试特定端，也可以手动分步启动：

#### 3.3.1 后端启动 (Python / FastAPI)
1. **环境准备**：确保已安装 [uv](https://docs.astral.sh/uv/)。
2. **配置环境**：`copy api/.env.example api/.env` 并填写 Key。
3. **运行**：
   ```powershell
   uv --directory api run uvicorn main:app --reload
   ```

#### 3.3.2 前端启动 (React / Vite)
1. **安装**：`cd web && npm install`
2. **启动**：分别运行 `npm run dev:doctor`, `npm run dev:tv`, `npm run dev:mobile` 或 `npm run dev`。